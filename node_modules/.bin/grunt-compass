#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-compass/bin/grunt-compass" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-compass/bin/grunt-compass" "$@"
  ret=$?
fi
exit $ret
