#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-img/bin/grunt-img" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-img/bin/grunt-img" "$@"
  ret=$?
fi
exit $ret
