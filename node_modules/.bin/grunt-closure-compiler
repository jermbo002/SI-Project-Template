#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-closure-compiler/bin/grunt-closure-compiler" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-closure-compiler/bin/grunt-closure-compiler" "$@"
  ret=$?
fi
exit $ret
